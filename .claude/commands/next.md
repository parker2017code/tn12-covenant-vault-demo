Show the current priority queue of next tasks for this TN12 lab.

1. Read `artifacts/next-work-queue.json` if it exists, otherwise run `npm run project:queue` to generate it.
2. Read `README.md` section "Current blockers".
3. Read `MEMORY.md` section "Current Next Work" if present.

Report: a numbered list of the top 5 open tasks with their status and why they're blocked (if blocked). Be specific — name the artifact, script, or SDK that's missing. No vague summaries.
